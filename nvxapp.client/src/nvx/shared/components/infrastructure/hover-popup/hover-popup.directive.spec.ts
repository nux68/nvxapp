import { HoverPopupDirective } from './hover-popup.directive';

describe('HoverPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverPopupDirective();
    expect(directive).toBeTruthy();
  });
});
