import { ParCausaliToLongTextPipe } from './par-causali-to-long-text.pipe';

describe('ParCausaliToLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ParCausaliToLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
