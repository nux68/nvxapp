import { StatoRichiestaShortTextPipe } from './stato-richiesta-short-text.pipe';

describe('StatoRichiestaShortTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StatoRichiestaShortTextPipe();
    expect(pipe).toBeTruthy();
  });
});
