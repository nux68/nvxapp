import { TipoTimbraturaToShortTextPipe } from './tipo-timbratura-to-short-text.pipe';

describe('TipoTimbraturaToShortTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoTimbraturaToShortTextPipe();
    expect(pipe).toBeTruthy();
  });
});
