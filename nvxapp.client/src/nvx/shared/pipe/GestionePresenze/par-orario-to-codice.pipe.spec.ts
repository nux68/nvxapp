import { ParOrarioToCodicePipe } from './par-orario-to-codice.pipe';

describe('ParOrarioToCodicePipe', () => {
  it('create an instance', () => {
    const pipe = new ParOrarioToCodicePipe();
    expect(pipe).toBeTruthy();
  });
});
