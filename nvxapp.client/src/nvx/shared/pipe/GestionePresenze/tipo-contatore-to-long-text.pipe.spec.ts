import { TipoContatoreToLongTextPipe } from './tipo-contatore-to-long-text.pipe';

describe('TipoContatoreToLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoContatoreToLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
