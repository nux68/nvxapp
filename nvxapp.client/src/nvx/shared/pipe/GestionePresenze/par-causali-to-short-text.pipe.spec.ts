import { ParCausaliToShortTextPipe } from './par-causali-to-short-text.pipe';

describe('ParCausaliToShortTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ParCausaliToShortTextPipe();
    expect(pipe).toBeTruthy();
  });
});
