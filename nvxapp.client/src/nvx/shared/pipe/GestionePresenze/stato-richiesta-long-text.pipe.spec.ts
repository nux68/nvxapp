import { StatoRichiestaLongTextPipe } from './stato-richiesta-long-text.pipe';

describe('StatoRichiestaLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StatoRichiestaLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
