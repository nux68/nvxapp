import { IdAspNetUsersToCognomePipe } from './id-asp-net-users-to-cognome.pipe';

describe('IdAspNetUsersToCognomePipe', () => {
  it('create an instance', () => {
    const pipe = new IdAspNetUsersToCognomePipe();
    expect(pipe).toBeTruthy();
  });
});
