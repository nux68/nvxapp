import { TipoRichiestaToShortTextPipe } from './tipo-richiesta-to-short-text.pipe';

describe('TipoRichiestaToShortTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoRichiestaToShortTextPipe();
    expect(pipe).toBeTruthy();
  });
});
