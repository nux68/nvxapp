import { TipoTimbraturaToLongTextPipe } from './tipo-timbratura-to-long-text.pipe';

describe('TipoTimbraturaToLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoTimbraturaToLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
