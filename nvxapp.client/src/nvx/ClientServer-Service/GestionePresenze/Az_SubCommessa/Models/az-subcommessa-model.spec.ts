import { Az_SubCommessaModel } from './az-subcommessa-model';
describe('Az_SubCommessaModel', () => {
  it('should create an instance', () => {
    const model = new Az_SubCommessaModel();
    expect(model).toBeTruthy();
  });
});
