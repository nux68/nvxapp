import { Dip_CompetenzaModel } from './dip-competenza-model';
describe('Dip_CompetenzaModel', () => {
  it('should create an instance', () => {
    const model = new Dip_CompetenzaModel();
    expect(model).toBeTruthy();
  });
});
